# App Engine fallback configuration (Cloud Run is the primary deployment path)
runtime: nodejs20
env: standard

entrypoint: node server.js

env_variables:
  NODE_ENV: "production"

# For Cloud Run, this file is not used.
